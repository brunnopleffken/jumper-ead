<h1>Jumper! Cursos e Idiomas</h1>

<p>
  <strong>Curso:</strong> Informática<br>
  <strong>Professor:</strong> Prof. Brunno Pleffken
</p>

<table class="table">
  <thead>
    <tr>
      <th>Nome do aluno</th>
      <th>Turma</th>
    </tr>
  </thead>
  <tbody>
    <% @students.each do |s| %>
      <tr>
        <td><%= link_to s.name, student_lessons_path(s) %></td>
        <td><%= s.classroom.weekday %>, <%= s.classroom.hour %></td>
      </tr>
    <% end %>
  </tbody>
</table>
